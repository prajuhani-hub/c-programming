#include<stdio.h>
int main(){
	int a,b,c;
	scanf("%d %d %d",&a,&b,&c);
	if((a<b)&&(a<b))
	printf("%d is minimum",a);
	else if(b<c)
	printf("%d is minimum",b);
	else
	printf("%d is minimum",c);
	return 0;
}